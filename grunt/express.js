module.export= {
            options: {
                // Override defaults here
            },
            dev: {
                options: {
                    script: 'proxy.js'
                }
            }
        };
